V62 — 함체 안에서 GIS·현장 선번 비교, 메모와 내역 수정

사용 순서: 2 전도면작성(현장반영) → 함체 또는 RN → 현장 선번조사 → A1에 Excel 붙여넣기 → 비교만 저장 → 다른 항목 선택 → 아래 비교 내용 확인 → 선택 연결·내역 직접 수정.

- '비교만 저장'은 실제 코어 연결과 내역을 유지하면서 조사표와 확인 결과만 저장합니다. 기존 '검사·저장 (신규 연결 추가)'은 별도 버튼으로 유지합니다.
- GIS 기준 연결·코어ID·코어명·상태·신호를 도면 안에 보존합니다. 기존 프로젝트는 처음 비교 저장할 때 GIS 기준본을 가져옵니다. GIS 기준본이 없으면 '조사 시작 도면 (GIS 기준본 없음)'으로 표시합니다.
- 행을 선택하면 GIS 기준 / 현장 조사 / 현재 수정 도면을 나란히 볼 수 있습니다. 'A 1번: GIS B 1번 → 현장 B 2번'처럼 각 방향에서 달라진 상대 케이블과 코어번호를 표시합니다.
- 같은 항목은 기존 연결을 유지하며 확인완료로 표시합니다. 다른 항목은 빨간색으로 남고, 수정 후에도 원래 GIS와의 차이는 지워지지 않습니다. '기존 유지', '수정 대기', '수정·확인 완료'를 구분합니다.
- 'GIS와 다른 항목' 필터는 수정이 끝난 차이도 다시 보여줍니다. 코어ID·코어명 변경 후에는 현장 확인을 다시 받습니다.
- 선택 행에 수정·확인 메모를 저장할 수 있습니다. 비교 내용 복사, 조사표 셀 수정, 양방향 전체 경로 확인을 같은 창에서 사용합니다.
- 서로 바뀐 번호는 Ctrl 키로 관련 조사 행을 함께 선택하고 '선택 연결·내역 직접 수정'을 누르세요. 각 연결에 사용할 GIS/현재/현장 내역을 직접 지정하고 수정 근거를 적습니다.
- '변경 범위·경로 확인'에서 해제할 연결, 새 연결과 실제 연결된 경로의 내역 변경 범위를 확인한 뒤 적용합니다. 미리보기·취소는 도면을 바꾸지 않습니다. 잠금·변경 감지·순환·중복 충돌은 반영을 보류합니다.
- 실제 연결된 변경 범위만 수정하며, 다른 곳에 같은 코어ID가 있다는 이유로 그 구간까지 바꾸지 않습니다. 기존 상태·메모가 다른 ID로 섞이지 않게 보존합니다.
- '수정이력·보존내역'에는 변경 전 내역과 수정 이유가 남습니다. 위치가 사라진 실제 내역은 원래 코어ID로 배정대기에 남겨 완료율에서 누락되지 않게 합니다. 새 경로 확인 또는 GIS 오기록 확인도 근거와 함께 기록합니다.
- 수정 전 자동백업하며 연결·내역 변경과 보존기록은 한 번의 실행취소/다시실행으로 함께 복원합니다. 재붙여넣기, 프로그램 재시작과 계정 도면 동기화에도 비교 기준·메모·보존내역을 유지합니다.

프로그램을 완전히 닫고 기존 실행.bat으로 다시 실행하면 자동 업데이트됩니다.

V61 — 전·후도면 필수 코어 완료율, 상태 편집 공통 표시 갱신

- 같은 코어ID의 상태를 한 케이블에서 변경하면 열려 있는 다른 케이블의 상태표·코어ID/코어명 표, 함체 양쪽 표, 함체 코어내역과 전체 코어내역에도 바로 반영합니다.
- 해지는 회색(#d9d9d9)으로 통일합니다. 미완료 표시나 오류 상태가 함께 있어도 해지 배경은 회색으로 표시합니다. 해지 해제와 실행취소·다시실행도 전체 창에 반영합니다.
- 상태를 갱신하면서 다른 창에서 입력 중인 코어ID·코어명·함체명과 Excel 수정 이력을 유지합니다.

완료율 = 필수 코어 중 정상 연결 완료 수 ÷ 필수 코어 수 × 100

| 구분 | 필수 연결 대상 | 전체 수·완료 수에서 제외 |
| --- | --- | --- |
| 전도면(GIS·현장반영) | 신호 ON 또는 실제 코어ID가 있는 코어 | 신호 없는 임시코어, 예외, 끊김(끊킴) |
| 후도면 | 신호 ON 또는 실제 코어ID 또는 해지예상 | 신호 없는 임시코어, 해지 |

- 제외 조건을 먼저 적용합니다. 임시코어라도 신호가 있으면 포함합니다. 후도면의 예외·끊김 코어도 ID나 신호가 있으면 필수 대상입니다.
- 같은 코어ID는 케이블 수와 관계없이 1개로 계산합니다. 코어ID 없는 신호·해지예상 코어는 번호별 미완료로 표시하여 누락을 막습니다. 대상이 0개이면 '대상 없음'으로 표시합니다.
- 완료율, 미완료 코어 목록, 필수 코어의 배정필요·연결대기, 차수별 연결 완료율에 같은 기준을 적용합니다. 현장 점검은 필수 코어를 확인하고 잘못된 조사 입력은 별도로 남깁니다.
- '완료율 대상·제외 기준 보기'에서 포함·제외 코어와 사유를 확인하고 행을 더블클릭해 경로/위치를 확인합니다.
- 실제 코어 접속을 따라 경로 분리·미접속·중복·분기와 양 끝을 검사합니다. RN은 내부 포트 접속이 있어야 정상 끝단입니다. 후도면에서는 철거·절단 구간을 유효 경로로 세지 않습니다.
- 현장반영 전도면 100% → 후도면 100%를 각각 확인합니다. 절체 작업 확인율은 별도 항목으로 유지합니다.
- 코어 상태에 '해지예상'을 추가했습니다.

프로그램을 닫고 기존 실행.bat으로 다시 실행하면 자동 업데이트됩니다.

V60 — 현장 Excel 조사표, 양방향 경로 확인, Ctrl+F 통합 찾기

- Ctrl+F 또는 찾기 버튼에서 코어ID·함체ID·케이블ID·RNID·시설명·RN 전산화번호로 검색합니다. 결과를 클릭하면 도면의 해당 위치로 이동하고 선택합니다. 코어ID는 서로 끊어진 구간을 포함한 전체 경로와 코어번호를 주황색으로 표시합니다.
- 연결대기확인의 ↔ 최종 확인과 양방향 경로 보기에서 선택한 코어부터 실제 접속을 따라간 양쪽 전체 경로를 표시합니다. 여러 건 연결도 경로 확인창을 거쳐 최종 반영합니다.
- 1번방향 주황, 2번방향 보라, 같은 케이블·코어 겹침 빨강, 같은 케이블의 다른 코어 겹침 갈색으로 구분합니다. 만나는 시설과 양쪽의 케이블ID·코어번호, 도달 끝을 표시합니다. 같은 시설 통과와 실제 접속을 구분하며 이미 이어진 경로·순환·분기·다른 ID 접속을 알립니다.
- 현장 조사 결과 행을 클릭하면 양방향 경로를 표시하고, 양방향 경로 버튼에서 자세히 볼 수 있습니다. 선택 현장대로 반영도 경로와 변경 내용을 확인한 후 적용합니다. 경로 조회만으로 연결은 바뀌지 않습니다.
- 선택 코어삭제는 해당 케이블의 선택 번호만 비우고 그 번호의 양 끝 접속만 해제합니다. 반대편 케이블의 코어 정보는 유지됩니다. 통과 연결에서 삭제하면 양쪽 함체의 배정필요가 각각 1개씩 다시 표시되는 동작을 검증했습니다.

프로그램을 닫고 기존 실행.bat으로 다시 실행하면 자동 업데이트됩니다.

- 현장 선번조사를 Excel표 일괄연결과 같은 A/B/C 열, 행 번호가 있는 셀 표로 바꿨습니다.
- 제목을 포함한 Excel 범위를 A1에서 Ctrl+V하거나 Excel 전체표 추가 버튼을 누르세요. 케이블 열 순서가 달라도 제목을 맞추며 이번 붙여넣기에 없는 조사 항목은 유지합니다.
- 기존 케이블표와 원본표(앞 6열: 코어ID / 코어명 / 회선번호 / 회선명 / 가입자명 / 중요여부)를 모두 받습니다. RN내부 열도 사용할 수 있습니다.
- 같은 코어의 새 조사 내용은 조사표에서 갱신하고 현재 도면과 비교합니다. 붙여넣기와 비교만으로 실제 연결을 바꾸지 않습니다.
- 검사·저장 (신규 연결 추가)을 누르면 일치 항목을 확인하고, 기존 연결과 겹치지 않는 신규 연결·비어 있는 정보를 추가합니다. 신규 추가 전 자동백업하며 한 번의 실행취소로 조사 저장과 추가를 함께 되돌립니다.
- 기존 연결·실제 코어ID·코어명과 다른 항목은 빨간색 불일치로 표시하고 기존 도면을 유지합니다. 범위 오류·중복 입력·잠금으로 추가할 수 없는 항목도 결과를 남깁니다.
- 빈칸 붙여넣기는 기존 셀 값을 유지합니다. 셀을 지우려면 Delete 또는 선택 셀 지우기를 사용하세요. 조사표 수정으로 기존 도면 연결을 삭제하지 않습니다.
- 셀 더블클릭으로 직접 수정하고 Enter/Tab으로 이동합니다. Ctrl+Z/Ctrl+Y는 입력표의 수정만 실행취소/다시실행합니다.
- 결과에서 선택 행 표에서 수정 또는 더블클릭으로 해당 조사 행을 고칠 수 있습니다. 기존 도면 연결을 바꾸려면 검사·저장 후 선택 현장대로 반영에서 변경 내용을 확인하고 적용하세요.
- 조사표와 미확인·불일치·이상 상태는 도면에 저장됩니다. 나중에 다시 열어 수정할 수 있으며 계정 동기화와 도면 실행취소에 포함됩니다. 수동으로 표시한 이상 사유도 재붙여넣기로 지워지지 않습니다.

사용 순서: 2 전도면작성(현장반영) → 함체 또는 RN → 현장 선번조사 → A1 붙여넣기 → 검사·저장 → 빨간 불일치는 나중에 표 수정 또는 선택 현장대로 반영.

V59의 말단 함체 현장 확인 제외, RN 내부 접속 기준, 자동 잠금과 잠긴 정보 복사를 유지합니다.
